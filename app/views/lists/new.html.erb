<%= form_with model: @list, url: '/lists', method: :post do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <%= link_to "編集", edit_list_path(@list.id) %> 
  
  <%= f.submit "投稿" %>
<% end %>